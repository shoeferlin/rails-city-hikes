// render the partial of the routes cards

function replaceResults() {
  const newCards = `<%= render 'shared/cards', routes: @routes %>`
  document.querySelector("#row-for-cards").innerHTML = newCards
}
replaceResults()
