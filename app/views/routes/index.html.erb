<h1>Routes#index</h1>
<p>Find me in app/views/routes/index.html.erb</p>

<% @routes.each do |route| %>
  <%= route.name %>
  <%= route.description %>
  <%= link_to "show", route_path(route) %>
<% end %>
