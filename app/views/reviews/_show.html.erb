<div class="show-review-box">
  <% reviews.each do |r| %>
  <div class="review">
    <p>Rating: <%= r.rating %></p>
    <p>Comment: <%= r.content %><p>
    <% unless current_user.nil? %>
      <p>From <%= r.user.username %></p>
    <% end %>
  </div>
  <% end %>
</div>
